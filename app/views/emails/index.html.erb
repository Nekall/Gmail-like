<nav class="navbar navbar-light bg-light">
  <span class="navbar-brand mb-0 h1"><span class="m-1"><i class="far fa-paper-plane fa-lg"></i> 𝙈𝙚𝙨𝙨@𝙜𝙚</span>  <sup><small style="font-size: x-small;">VOTRE MESSAGERIE SECURISE</small></sup></span>
  <button class="btn btn-outline-dark m-1" type="button" name="button"><%= link_to 'Créer un mail', new_email_path, remote: true %></button>
</nav>
<hr class="m-0">
<div class="container m-0">
  <div class="row align-items-top">
    <div class="col-4">
      <table class="table table-hover">
        <tbody id="newMail">
          <% @emails.each do |email| %>
          <tr>
            <td id="delete<%= email.id %>"><%= link_to email.object, email, remote: true %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="col-8">
      <div class="container m-2" id="contentEmail">
        <div class="row align-items-center" style="text-align: center;">
          <p><i class="fas fa-shipping-fast fa-3x"></i></p>
        </div>
      </div>
    </div>
  </div>
</div>
<!--
<td><#%= link_to 'Show', email %></td>
<td><#%= link_to 'Edit', edit_email_path(email) %></td>
-->
